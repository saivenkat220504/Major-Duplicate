import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.smartairport.assistance',
  appName: 'Smart Airport Assistance',
  webDir: 'dist'
};

export default config;
